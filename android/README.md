# WishTrace Android

Native Android client for the WishTrace gifting flow.

This directory contains only the presentation, navigation, local UI state, and
replaceable client-side interfaces. OpenAI, merchant, and Prava secrets and
authoritative transaction state belong on the backend.

The current data source is an explicitly controlled seeded demo implementation.
